<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate %>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Race</th>
      <th>Age</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.type %></td>
        <td><%= link_to 'Show', user_path(user.id) %></td>
        <td><%= link_to 'Edit', user_path(user.id, user, :edit) %></td>
        <td><%= link_to 'Destroy', user_path(user.id, user), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to "See all #{user.type.pluralize}", user_path(user.type) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
 -->

